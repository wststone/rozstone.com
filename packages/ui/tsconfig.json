{
	"extends": "tsconfig/nextjs.json", //"tsconfig/react-library.json"
	"include": ["."],
	"exclude": ["dist", "build", "node_modules"],
	"compilerOptions": {
		"paths": {
			"@shared": ["./shared"],
		}
	}
}
